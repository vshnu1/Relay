# Model artifacts

Only artifacts trained exclusively on synthetic data may live here, and each
must be labelled synthetic in its metadata file. Anything trained on private
data belongs under the gitignored `data/` tree and is never committed.
