# Python dependencies for ml/relay_ml. Not needed to build or run the web app
# — the demo UI computes client side and the server uses shared/engine.js.
# Required only if the ML engine is wired into a service. See docs/ML-WIRING.md.
numpy>=1.24
# Pinned to the release ml/artifacts/synthetic-prior-*.joblib was pickled with;
# a newer release cannot apply the prior and the model falls back to 'unavailable'.
scikit-learn>=1.5,<1.6
